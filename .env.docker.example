# Docker Database Configuration Example
# Copy this file to .env.docker and update with your desired credentials
# These credentials are used specifically for the PostgreSQL container in Docker Compose

# PostgreSQL container configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=your_database_name_here